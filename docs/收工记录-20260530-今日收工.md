# 今日收工 — 2026-05-30 ～ 05-31

> 真源：[HANDOFF.md](./HANDOFF.md) · [验收大纲-自动与必做.md](./验收大纲-自动与必做.md) · [批次 K](./收工记录-20260530-批次K.md)

## 今日完成（Agent + 维护者）

| 阶段 | 摘要 |
|------|------|
| **I / J** | 探针门禁 · 枪战 8/8 + J4 · Codegen UX · `git push` |
| **K** | 鸿蒙实体列表 Supabase REST · `c8006cb` / `448d52f` |
| **K+部署** | `dpl_JBiWLbLoaofPqx3hGHacude43bii` → https://app-factory-five.vercel.app |
| **维护者·生产 UI** | 枪战 `0ea7a53c-a645-4ad9-a43a-02263f9b7b4a` 8/8 + 三栈已完成；下载 `kids_gunfight-harmony` |
| **维护者·本地探针** | `verify:s6:local-full` ✅（`faa13b44-…` · wechat build passed） |

## 生产与样本真源

| 项 | 值 |
|----|-----|
| 生产站 | https://app-factory-five.vercel.app |
| **最新部署** | `dpl_JBiWLbLoaofPqx3hGHacude43bii` |
| 枪战（生产） | `0ea7a53c-a645-4ad9-a43a-02263f9b7b4a` |
| S6 待办 | `802e1144-57ef-4ff4-b33c-325ebad299ce` |
| 本地联调 | `cd "/Users/guiming/Desktop/app生产工厂/app-factory"` · 3001 + Inngest 8288 |

## 维护者备忘（终端）

- **`npm run` 必须在 `app-factory` 目录**，不是 `~` 或下载的 `kids_*-harmony`。
- `cd` 与路径之间要有空格：`cd "/Users/guiming/Desktop/app生产工厂/app-factory"`。
- `verify:v3:production` = 测 **Vercel 生产**；`verify:s6:local-full` = 测 **本地 3001**（已通过）。

## 维护者必做

**无。** `maintainer:pending` → 0。

## 可选（明日 / 有空）

| 项 | 命令 / 动作 |
|----|-------------|
| 生产 HTTP 全链路 | `npm run verify:v3:production`（在 app-factory 下） |
| DevEco 打开枪战鸿蒙 ZIP | 看 `Index.ets` 是否含 `loadItems` |
| 鸿蒙注入 Supabase 常量 | 批次 L |
| 体验 E1–E5 | 微信持久化 / 扫码预览等 |

## 明日开工（批次 L）

```bash
cd "/Users/guiming/Desktop/app生产工厂/app-factory"
npm run verify:i0:batch
```

1. codegen 注入鸿蒙 `SUPABASE_URL` / `ANON_KEY`（或文档说明 `--dart-define` 等价物）
2. 本机 `verify:v3:production`（网络允许时）
3. 观察 K+部署 后新 run 的 harmony 成功率（`stats:codegen`）

## 收工判定

**✅ 可收工**：代码已 push · 生产已部署 · 枪战三栈 UI 已验收 · 本地 S6 全链路已通过。
