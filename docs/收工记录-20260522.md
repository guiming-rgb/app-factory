# 收工记录 — 2026-05-22

> **用途**：下次开工先读本文 + [执行计划.md](./执行计划.md) §二。  
> **仓库**：`https://github.com/guiming-rgb/app-factory` · `main` @ **`a2de8ae`**（远端；本地或有未提交文档/脚本）。

---

## 一、今日做了什么

| 事项 | 结果 |
|------|------|
| 开工阅读 HANDOFF / 执行计划 / codegen 文档 | ✅ |
| `npm run check:codegen:table` | ❌ 仍报 `codegen_runs` 不在 schema cache |
| 排查 Supabase 项目对错 | ✅ 根因：**维护者进错项目** |
| 向维护者说明「关终端 ≠ 丢表」 | ✅ |
| 逐步说明 Dashboard 操作（SQL Editor、Settings→API） | ✅（维护者尚未确认已在 **dllaezdyxmoebkkwbftd** 完成） |
| 新增 Agent 辅助脚本 | `check:codegen:table`、`db:apply:codegen`（需 `DATABASE_URL`） |
| G10 / G9-Docker / 异步 codegen 联调 | ⏸ 待 P0 通过 |

### 关键结论（务必记住）

**工厂程序**（`.env.local`）只连：

- **Reference ID**：`dllaezdyxmoebkkwbftd`
- **域名**：`dllaezdyxmoebkkwbftd.supabase.co`
- **Dashboard 卡片名**：**guiming-rgb's Project**（东京 ap-northeast-1）

**不是**工厂用的项目（勿在此跑迁移）：

- **Reference ID**：`ypvbdwyeiidwmgxylhwn`
- **卡片名**：**guiming@263.net**（美西 us-west-2）

在此项目跑 SQL **不会**让工厂 G10 通过。

---

## 二、进度百分率（截至收工）

| 维度 | % | 说明 |
|------|---|------|
| **全产品长线**（方案→代码→测试→部署→运营） | **38%** | v3～v6 未开始 |
| **当前 MVP 主线**（8-Agent + 双轨 ZIP 导出） | **92%** | 稳定 |
| **Inngest codegen**（表 + G10 + 异步联调） | **65%** | 代码在仓；**库表未在正确项目就绪** |
| **v2.1 沙箱** | **55%** | 本机 G9 ✅；Docker 脚本有、本机/Agent 环境未验 |
| **可演示工厂** | **75%** | 缺 codegen 验收与 UI 按钮 |

---

## 三、阻塞与易错（下次先扫一眼）

1. **P0（唯一硬阻塞）**：在 **`dllaezdyxmoebkkwbftd`** 执行 `sql/migrations/20260520_codegen_runs.sql`，并 **Settings → API → Reload schema**。
2. **勿在 `ypvbdwyeiidwmgxylhwn` 操作**（即使用户称「SQL 已跑」）。
3. **关终端**不影响 Supabase 表；**未 Reload schema** 或 **项目错了** 才会一直报错。
4. 异步 codegen 需双进程（3001 + `inngest:dev:3001`），与建表无关。

---

## 四、下次开工顺序（复制即用）

### 维护者（约 5 分钟，仅 P0）

1. https://supabase.com/dashboard → 点 **guiming-rgb's Project**（勿点 guiming@263.net）。
2. 地址栏含 **`dllaezdyxmoebkkwbftd`**。
3. **SQL Editor** → 自检 `codegen_runs` → 无则跑 `sql/migrations/20260520_codegen_runs.sql`。
4. **Settings → API → Reload schema**。
5. 对话回复：**「项目是 dllaezdyxmoebkkwbftd，已 Reload」**。

**可选**：`.env.local` 增加 `DATABASE_URL` → 说 **「已加 DATABASE_URL」** → Agent 跑 `npm run db:apply:codegen`。

### Agent（维护者回复后）

```bash
cd "/Users/guiming/Desktop/app生产工厂/app-factory"
npm run check:codegen:table
npm run verify:codegen:flutter -- 833ad678-f204-40d7-a47c-5b76e803f64f
# 通过后：双进程 + POST .../codegen/flutter 联调
```

### 样本与端口

- 项目 ID：`833ad678-f204-40d7-a47c-5b76e803f64f`
- Web：`http://localhost:3001`

---

## 五、本地未提交变更（若有）

可能含：`docs/执行计划.md`、`docs/HANDOFF.md`、`docs/ONE_PAGER.md`、`scripts/check-codegen-table.ts`、`scripts/apply-codegen-migration.mjs`、`package.json`（`check:codegen:table`、`db:apply:codegen`）。下次可一并 commit。

---

## 六、变更记录

| 日期 | 内容 |
|------|------|
| 2026-05-22 | 本收工记录；明确双 Supabase 项目；P0 仍为 codegen_runs 迁移 |
