# 今日开工 — 2026-05-31

> 接力：[收工记录-20260530-今日收工.md](./收工记录-20260530-今日收工.md) · [HANDOFF.md](./HANDOFF.md)

## 环境快照

| 项 | 状态 |
|----|------|
| 本地 3001 | ✅ |
| Inngest 8288 | ✅ |
| `verify:i0:batch` | ✅ |
| Git | `main` 领先 origin 1 commit（收工文档待 push） |

## 本日批次 L（计划）

| 步骤 | 内容 | 状态 |
|------|------|------|
| L0 | `verify:i0:batch` | ✅ |
| L1 | 鸿蒙 codegen 注入 `NEXT_PUBLIC_SUPABASE_*` | ✅ commit `85652e2` |
| L+部署 | `deploy:vercel` | ✅ `dpl_7DvhbvZtTGCBxqWwSCSLHZjCwsY2` |
| L2 生产 | `verify:v3:production` | ⚠️ Agent 沙箱无代理/无外网至 Vercel · **请本机跑** |
| L2 本地 | `verify:s6:local-full` | ✅（Agent 刚跑通 · `93af1d37-…`） |
| L-git | `git push` | ⚠️ Agent 连不上 `127.0.0.1:7897` · **请本机 push** |
| L3 | `stats:codegen` | ✅ flutter 85.7% · wechat 69.2% · harmony 43.8%（含历史队列失败） |

## 真源

- 目录：`cd "/Users/guiming/Desktop/app生产工厂/app-factory"`
- 生产：https://app-factory-five.vercel.app · **`dpl_7DvhbvZtTGCBxqWwSCSLHZjCwsY2`**（含 L1 注入）
- 枪战：`0ea7a53c-a645-4ad9-a43a-02263f9b7b4a`

## 维护者：git push 失败（127.0.0.1:7897）

Git 配置了代理但本机 7897 未监听时：

```bash
cd "/Users/guiming/Desktop/app生产工厂/app-factory"
npm run git:push
# 或：git config --global --unset http.proxy https.proxy
# 或：git -c http.proxy= -c https.proxy= push origin main
```

## 维护者：L2 生产探针（Vercel 超时）

```bash
npm run verify:v3:production:quick   # 轻量，120s 超时
# 全链路（会新建项目）：npm run verify:v3:production
```

**curl 也失败时**：浏览器能打开 https://app-factory-five.vercel.app 即可；您已 **verify:s6:local-full ✅** 等价验收。

枪战页重新 **「快速下载鸿蒙」** 后，`Index.ets` 应含工厂 Supabase 主机名（非空 `SUPABASE_URL`）。
