# 验收大纲 — 自动验收 vs 维护者必做

> **原则**：能 Agent/脚本跑的 **不打扰您**；只把 **必须您动手** 或 **需要您亲自体会成果** 的写进「维护者清单」。  
> **最后更新**：2026-05-30（批次 H 后 · 自动探针已跑通）

---

## 一、Agent 已自动验收（2026-05-30 本轮）

以下 **已全部通过**，无需您重复做：

| 探针 | 命令 | 结论 |
|------|------|------|
| E2 待办三栈 | `npm run verify:todo:parity` | ✅ |
| G2 实体列表 | `npm run verify:g2:entity-scaffold` | ✅ |
| H3 持久化 | `npm run verify:g3:persistence` | ✅ |
| H4 枪战 Spec 本地 | `npm run verify:h4:shooter` | ✅ |
| C6 鸿蒙 | `npm run verify:c6:harmony` | ✅ |
| C4 push 单元 | `npm run verify:c4:github:push-unit` | ✅ |
| 维护者清单 | `npm run maintainer:pending` | ✅ 0 项 |
| 生产构建 | `npm run build` | ✅（每批改动后 Agent 自跑） |

**生产部署**：https://app-factory-five.vercel.app（最新批次 H：`dpl_6Qx4Cu7Z8DbQz4uULWRcuEbPnrWt`）

**已确认过的维护者成果（对话 + 截图，不再重复要求）：**

- S6 待办：P0、小程序模拟器、三栈 GitHub、`simple_todo-wechat` 仓库

---

## 二、维护者必做（仅 2 项 · 加快进度用）

| 优先级 | 事项 | 为何必须您 | 预计 |
|--------|------|------------|------|
| **B1** | `git push origin main` | 本机 GitHub HTTPS 凭证 | 1 min |
| **B2** | 枪战项目 **8/8 Agent**（可选，开新业务时） | 需在生产站点击生成 | 10～30 min |

### B1 — 推送代码

```bash
cd "/Users/guiming/Desktop/app生产工厂/app-factory"
git push -u origin main
```

### B2 — 枪战样本（仅当要做 H4 生产验收时）

1. 打开 https://app-factory-five.vercel.app/projects/0ea7a53c-a645-4ad9-a43c-02263f9b7b4a  
2. **启动 AI 后台生产** → 等到 **8/8**  
3. 回复 Agent：`枪战 8/8：✅`（不必下载三栈，除非您想体验）

---

## 三、体验验收（可选 · 有成果再自己做）

**不阻塞发版/下一批次**；想亲自感受时再做的 **短清单**：

| 项 | 做什么 | 通过标准 |
|----|--------|----------|
| E1 | 生产站重新 **快速下载小程序** → 导入微信工具 | 关工具再开，任务还在 |
| E2 | 三按钮均为 **「生成 xxx ZIP（同步）」** 且点一次能 **已完成** | 不再长期排队 |
| E3 | 微信 **预览** 扫码 | 与模拟器一致 |
| E4 | DevEco 打开下载的 `simple_todo-harmony` Run | 添加/删除任务 |
| E5 | Flutter `flutter run` 待办页 | 持久化生效 |

做完某项可在对话说一句，例如 `E1 持久化：✅`；**不做也视为批次 H 代码验收通过**。

---

## 四、下一批次执行大纲（Agent 自驱动）

### 顺序链（Agent 默认）

```
I0 合并探针门禁到 CI 脚本
  → I1 Flutter analyze 沙箱策略 / 成功率统计
    → I2 枪战 8/8 后生产三栈抽样（您完成 B2 后）
```

### 并行（不打扰您）

| 组 | 内容 | 执行方 |
|----|------|--------|
| ∥-doc | HANDOFF / ONE_PAGER 同步 | Agent |
| ∥-ux | Codegen 失败行折叠、GitHub 链复制 | Agent |
| ∥-harmony | 鸿蒙列表接 Supabase REST（非待办） | Agent |

### 仅当您完成后 Agent 继续

| 触发 | 后续 |
|------|------|
| 您说 `枪战 8/8：✅` | Agent 跑生产 export + 对比实体列表 |
| 您说 `git push：✅` | Agent 在 PR/主线继续 I 批次 |

---

## 五、一键复验（维护者/Agent 均可）

```bash
cd "/Users/guiming/Desktop/app生产工厂/app-factory"
npm run verify:todo:parity && \
npm run verify:g2:entity-scaffold && \
npm run verify:g3:persistence && \
npm run verify:h4:shooter && \
npm run verify:c6:harmony && \
npm run maintainer:pending
```

**发版前 Agent 另跑**：`npm run build` → `npm run deploy:vercel`

---

## 六、样本与链接真源

| 用途 | ID / URL |
|------|----------|
| S6 待办（已验收） | `802e1144-57ef-4ff4-b33c-325ebad299ce` |
| 枪战（待 8/8） | `0ea7a53c-a645-4ad9-a43c-02263f9b7b4a` |
| 生产 | https://app-factory-five.vercel.app |
| 小程序 GitHub | https://github.com/guiming-rgb/simple_todo-wechat |
| Flutter GitHub | https://github.com/guiming-rgb/local_todo-flutter |
| 鸿蒙 GitHub | https://github.com/guiming-rgb/simple_todo-harmony |

**注意**：GitHub 链接不要带中文备注（如「（已有）」），否则会 404。

---

## 七、收工判定（当前）

| 维度 | 判定 |
|------|------|
| 代码/探针 | ✅ 批次 H 完成 |
| 生产部署 | ✅ |
| 维护者阻塞项 | 仅 **B1 git push**（可选 **B2** 枪战） |
| 体验项 | 全部可选 |

**结论**：**可进入下一批次 I**，无需再等小程序/鸿蒙真机 unless 您要 E1–E5 体验。
