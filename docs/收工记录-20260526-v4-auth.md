# 收工记录 — 2026-05-26（v4 Auth 启动日）

> **上一里程碑**：[收工记录-20260526-v3收工.md](./收工记录-20260526-v3收工.md)  
> **下一里程碑**：v4-3 API owner 校验 → v4-4 RLS  
> **设计真源**：[v4-Auth-RLS-设计草案.md](./v4-Auth-RLS-设计草案.md)

---

## 一、今日完成项

### 1.1 v4-1 Auth UI + Session（代码完成，本地 build ✅）

| 项 | 说明 |
|----|------|
| 依赖 | `@supabase/ssr` |
| 配置门控 | `lib/auth-config.ts` — 未配 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 时向后兼容 |
| Supabase 客户端 | `lib/supabase/browser.ts` · `server.ts` · `middleware.ts` |
| 路由 | `/login` · `/signup` · `/auth/callback` · `/auth/signout` |
| 组件 | `LoginForm` · `SignupForm` · `AuthHeader` |
| Middleware | 保护 `/projects/*`；已登录访问 `/login`/`/signup` 跳首页 |
| 联动 | `ProjectForm` 启用 Auth 时未登录先跳登录；`sync-vercel-env` / `deploy/status` 检查 anon key |

### 1.2 v4-2 owner_id 迁移 + 写入（代码完成，本地 build ✅）

| 项 | 说明 |
|----|------|
| SQL | `sql/migrations/20260526_v4_owner_id.sql`；`schema.sql` 同步 |
| 策略 A | 历史 `owner_id = null` 保留；登录用户列表/详情仅见自己的项目 |
| API | `POST /api/projects` 写 `owner_id`；Auth 启用时未登录 → **401** |
| API | `GET /api/projects` Auth 启用时按 `owner_id` 过滤 |
| 页面 | `listProjectsForPage` · `getProjectDetailForPage` 同步 owner 过滤 |
| 脚本 | `npm run db:apply:v4-owner` · `npm run check:v4:owner` |
| 辅助 | `lib/auth/api-user.ts` |

### 1.3 文档

- 新增 [v4-Auth-RLS-设计草案.md](./v4-Auth-RLS-设计草案.md)（上午 v3 收工时立项）
- 更新 HANDOFF · ONE_PAGER · 执行计划 · 安全审计（部分）

### 1.4 验证

```bash
npm run build   # ✅ 通过（v4-1 + v4-2 合入后）
```

---

## 二、今日未完成 / 未推送

| 项 | 状态 | 说明 |
|----|------|------|
| **Git commit & push** | ❌ 未做 | v4-1 + v4-2 全在本地工作区；远程仍 @ `bff6e0b` |
| **Supabase 跑 owner 迁移** | ❌ 维护者 | `npm run db:apply:v4-owner` 或 SQL Editor |
| **Vercel 配 anon key + Redeploy** | ❌ 维护者 | 生产 Auth 尚未生效 |
| **v4-3～v4-6** | ❌ | 见 §五 明日计划 |
| **v3 尾项** | ⏸ | 生产 generate 浏览器冒烟 · Inngest Serve URL 确认 |
| **重复文件清理** | ⏸ | `lib/inngest/client 2.ts` · `functions 2.ts` 应删 |

---

## 三、Git 工作区快照（收工时）

- **已改**：18 文件（middleware、projects API、package.json 等）
- **未跟踪**：约 37 项（login/signup/auth、v4 迁移脚本、设计草案等）
- **远程锚点**：`bff6e0b` test(v3): 生产验收脚本与云端 GET 验收记录

**明日第一件事建议**：整理 commit（v4-1 + v4-2 可一条或两条）并 push → Redeploy。

---

## 四、完工率评估（2026-05-26 收工口径）

### 4.1 按「已规划里程碑」

| 范围 | 完工率 | 说明 |
|------|--------|------|
| **MVP v1 → v3** | **100%** | 方案生产 · 可观测 · 双轨 codegen · Vercel 生产部署 · 终端验收脚本齐全 |
| **v4 公网安全线**（6 子阶段） | **≈33%**（2/6） | v4-1 UI ✅ · v4-2 owner ✅；v4-3 API 校验 · v4-4 RLS · v4-5 Inngest · v4-6 限流 待做 |
| **v5 记忆与技能** | **0%** | 未立项实施 |
| **v6 插件与生态** | **0%** | 后置 |

### 4.2 按「产品能力」

| 能力 | 状态 |
|------|------|
| 想法 → 8 Agent 方案报告 | ✅ 本地 + 生产可用 |
| usage_logs / 成本可观测 | ✅ |
| Flutter + 小程序 codegen + Storage + 预览 | ✅ PoC 级 |
| 自动修错 · Spec 收紧 · 小程序结构门禁 | ✅ |
| 生产 URL 可访问 | ✅ https://app-factory-five.vercel.app |
| **登录与项目归属** | 🟡 代码就绪，**未 push、未配生产 env、未跑 DB 迁移** |
| **API/RLS 防跨用户访问** | ❌ v4-3～v4-4 |
| **防刷接口 / 限流** | ❌ v4-6 |

### 4.3 综合结论（一句话）

> **「工厂核心闭环（v1→v3）已完工并可演示；作为公网产品「可长期安全运营」约完成三分之一（v4 六步中两步），整体长期路线图（至 v6）约七成在「已交付里程碑」层面，但 v5/v6 尚未启动。**

更直白：

- **做 Demo / 内测 / 自己用**：≈ **90%+**（缺生产 Auth 配置与迁移执行）
- **公网无人值守上线**：≈ **30%**（v4 安全线 2/6，且生产未部署 v4）
- **愿景终点（记忆·插件·生态）**：≈ **15～20%**（v1-v3 是主干，v5-v6 几乎未动）

---

## 五、明日开发计划（2026-05-27 建议）

### P0 — 必须先做（约 1～2h）

| # | 任务 | 负责 | 验收 |
|---|------|------|------|
| 1 | **commit & push** v4-1 + v4-2 | Agent | `main` 含 Auth + owner_id 代码 |
| 2 | Supabase 执行 `db:apply:v4-owner` | 维护者 | `npm run check:v4:owner` ✅ |
| 3 | Vercel 增加 `NEXT_PUBLIC_SUPABASE_ANON_KEY` + Redeploy | 维护者 | 生产 `/login` 可打开 |
| 4 | 删 `lib/inngest/* 2.ts` 重复文件 | Agent | 工作区干净 |

### P1 — v4-3 实施（Agent 主做，约半天）

| # | 任务 | 验收 |
|---|------|------|
| 5 | 抽取 `assertProjectOwner(projectId, userId)` 复用于 API | 单测或脚本可调用 |
| 6 | 以下路由加 owner 校验（非 owner → **404**） | 见设计草案 §6 |
| | `GET/POST /api/projects/[id]/*`（generate、export、codegen、spec、usage 等） | |
| 7 | 补 `verify:v4:owner-api` 或扩展现有验收脚本 | Agent 终端可跑 |
| 8 | `npm run build` + 文档勾选 v4-3 | |

**v4-3 路由清单（实施时逐项打勾）**：

- [ ] `GET /api/projects/[id]`
- [ ] `POST /api/projects/[id]/generate`
- [ ] `GET/POST .../export*` · `codegen/*` · `spec` · `usage`

### P2 — 可选（时间允许）

| # | 任务 |
|---|------|
| 9 | v3 尾项：生产浏览器 generate 冒烟 |
| 10 | Inngest Dashboard 确认 Serve URL |
| 11 | 维护者拍板 v4 草案 §11（历史 null 策略 / Magic Link vs 密码 / 临时 Deployment Protection） |

### 明日不开工项（刻意延后）

- **v4-4 RLS** — 依赖 v4-3 API 层稳定后再切 anon client
- **v4-5 Inngest userId** — 与 generate 鉴权同批改较稳
- **v5/v6** — 未排期

---

## 六、维护者明日最小清单

1. 确认 Supabase 项目仍为 **`dllaezdyxmoebkkwbftd`**
2. 配好 `DATABASE_URL`（若尚未）→ `npm run db:apply:v4-owner`
3. Supabase Dashboard → Authentication → 开启 Email（或拍板 Magic Link）
4. Vercel → Environment Variables → `NEXT_PUBLIC_SUPABASE_ANON_KEY`（与 URL 同项目）
5. Redeploy 后浏览器：注册 → 登录 → 新建项目 → Supabase 表内 `owner_id` 非空

---

## 七、风险与提醒

- **生产现状**：https://app-factory-five.vercel.app **仍是无 Auth 的旧部署**（`bff6e0b`），在 v4 push + env + redeploy 前，公网风险不变。
- **历史项目**：策略 A 下，启用 Auth 后旧项目（`owner_id null`）对登录用户不可见；如需可见需单独回填策略（§11 待拍板）。
- **样本项目 ID**（验收用）：`833ad678-f204-40d7-a47c-5b76e803f64f`

---

## 八、相关命令速查

```bash
# 本地
npm run build
npm run db:apply:v4-owner
npm run check:v4:owner

# 门禁（v4 前基线）
npm run verify:codegen:flutter -- 833ad678-f204-40d7-a47c-5b76e803f64f
npm run verify:codegen:wechat -- 833ad678-f204-40d7-a47c-5b76e803f64f
npm run verify:v13 -- 833ad678-f204-40d7-a47c-5b76e803f64f
npm run verify:v3:production

# 部署
npm run deploy:vercel:env
npm run deploy:vercel
```
