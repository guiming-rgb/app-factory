# App 生产工厂 — 完整执行计划 & 每日开工习惯

> **本文件是「总计划」**：每天开工先读这里，再读「上次进展」，再执行「下一步」。  
> **开发纲要（目标 / 约束 / 联调精华）**：[开发纲要.md](./开发纲要.md)  
> **安全审计与清单**：[安全审计与清单.md](./安全审计与清单.md)  
> 配套技术细节见：[项目功能与架构交接.md](./项目功能与架构交接.md)

---

## 一、每日开工习惯（建议固定顺序）

养成顺序，减少上下文丢失：

| 顺序 | 做什么 | 读哪里 |
|------|--------|--------|
| **1** | 看**总计划**：当前阶段目标、本阶段验收标准、是否在轨 | **[开发纲要.md](./开发纲要.md)**（浓缩）+ **本文「三、完整执行计划」+「四、阶段总览表」** |
| **2** | 看**昨天 / 上次会话**做了什么、遗留问题 | **本文「二、上次进展（滚动更新）」** |
| **3** | 定**今天**要完成的 1～3 个可交付项，收工前更新「上次进展」 | 同上第二节 + Git 提交说明 |

**收工前 5 分钟**：更新「二、上次进展」的日期、完成项、阻塞项、明日第一条动作。

---

## 二、上次进展（滚动更新）

> **每次干完活改这一块**，下次开工第一眼就能看到接力点。

| 字段 | 内容 |
|------|------|
| **更新日期** | 2026-05-12（今日收工） |
| **阶段状态** | **`feature/v1.2-inngest` = v1.2 联调候选版 / 代码冻结候选**（不再加大功能，先双进程联调）。`main` + `mvp-v1.1` = 同步版回退；`mvp-v1.2-code`（可选再标 `v1.2-code-ready`）= 异步候选快照。 |
| **上次完成** | 安全复查通过；`executeProjectWorkflow` 对 **非 running**（含 pending/failed）改为 **skipped 返回**而非 throw，避免过期 Inngest 事件触发外层 catch 把项目误标 **failed**。`docs/版本与分支.md`、标签、README 入口齐全。 |
| **待真机验收** | **唯一优先级**：`npm run dev`（建议 `-p 3000`）+ `npm run inngest:dev`（端口对齐）→ 走通 8 Agent → Supabase 三条验收 SQL → 再议是否合并 `main`。 |
| **已知阻塞** | 无代码阻塞。联调若「running 无 runs」：先查 Inngest 控制台与 `/api/inngest` URL。 |
| **下次开工第一件事** | 双进程联调；若第一条报错，收集 Inngest 控制台 + 终端 + `projects`/`agent_runs` 查询结果再修。 |

---

## 三、完整执行计划（从现状到「App 生产工厂」长期目标）

### 3.1 愿景（终点形态）

用户从 **App 想法** 出发，在平台内完成：**方案 → 代码 → 测试修复 → 仓库/部署 →（可选）运营迭代**，多智能体 + 工具 + 沙箱 + 评测 + 人工审核节点，**可替换模型与执行器**，不绑死单一框架。

### 3.2 原则

- **模板 + 评测 + 人类在环**：关键节点可确认；质量可观测。
- **可插拔**：编排 / 模型 / 工具（含 MCP）可演进。
- **先闭环再扩张**：每一阶段有可演示的端到端结果。

---

## 四、阶段总览表（里程碑）

| 阶段 | 名称 | 目标 | 状态 |
|------|------|------|------|
| **MVP v1** | 方案生产器 | 8 Agent 串行、落库、详情页、同步 generate | ✅ 已完成 |
| **MVP v1.1** | 体验与稳健性 | 轮询、导出、复制、历史、进度、重试策略、409、安全 body | ✅ 已完成 |
| **验收 A** | 真机联调 | 真实 Supabase + LLM 全链路通过，按验收清单打勾 | ⏳ 待你做 |
| **MVP v1.2** | 异步队列 | Inngest：`prepare` + 事件 + `execute`；前端轮询保持 | ✅ 联调候选（冻结加功能前，见「二、上次进展」） |
| **MVP v1.3** | 可观测与成本 | 任务日志、Token/耗时、限流、简单 Admin 或表盘 | 🔲 未开始 |
| **MVP v2** | 代码生成 | 模板选型、生成文件、ZIP、可选 GitHub | 🔲 未开始 |
| **MVP v2.1** | 沙箱验证 | 安装依赖、build/test、错误回传、有限自动修 | 🔲 未开始 |
| **MVP v3** | 部署闭环 | 预览环境、环境变量、部署日志 | 🔲 未开始 |
| **v4** | 用户与协作 | Supabase Auth、项目归属、团队版可后置 | 🔲 未开始 |
| **v5** | 记忆与技能 | 用户/项目记忆、Skill 版本、审核后发布 | 🔲 未开始 |
| **v6** | 插件与生态 | MCP/工具市场、可选 OpenClaw/Hermes 执行器 | 🔲 后置 |

---

## 五、分阶段说明（要做什么 / 验收什么）

### 阶段：验收 A（真机联调）

- **做**：配置 `.env.local`；执行 `schema.sql` / `seed.sql`；`npm run dev` 跑完整生成；测 v1.1 功能；可选错误 Key 测 failed 重试。
- **验收**：`projects.status=completed`、`final_report` 有长度、8 条 `agent_runs` 均为 `completed`、无重复 runs（除主动 forceRegenerate）、导出/复制/历史/409 符合预期。
- **产出**：验收记录（可写在「二、上次进展」或单独 `docs/验收记录.md`）。

### 阶段：MVP v1.2（异步队列 — 建议 Inngest）

- **做**：引入 Inngest（或 Trigger.dev）；`POST /generate` 只投递事件并返回 `jobId`/`accepted`；函数内 `step.run` 按 Agent 拆分；失败策略与幂等；DB 状态与现有一致。
- **验收**：关闭浏览器后任务仍完成；无长时间阻塞 HTTP；失败可重试且不破坏数据一致性。
- **依赖**：验收 A 通过后再做，避免异步层掩盖配置问题。

### 阶段：MVP v1.3（可观测与成本）

- **做**：`usage_logs` 或等价表、每步耗时、可选 token 统计；简单查询或页面。
- **验收**：能回答「单次生成花多久、调了几次模型」。

### 阶段：MVP v2（代码生成）

- **做**：固定技术栈模板；开发 Agent 输出文件树 + 写入存储或对象存储；ZIP 下载；可选 GitHub OAuth + push。
- **验收**：下载包可本地 `install && build`（在约定模板下）。

### 阶段：MVP v2.1～v3

- **做**：Docker/隔离沙箱、测试命令、错误回传迭代；一键预览部署（如 Vercel）。
- **验收**：从想法到可访问预览链接（在约定范围内）。

### 阶段：v4～v6

- **做**：登录与权限、记忆与技能版本化、工具/MCP 与执行器插件化。
- **验收**：按产品定义分项验收（上线前须合规与隐私评审）。

---

## 六、本周建议节奏（可随验收结果调整）

| 天 | 建议重点 |
|----|----------|
| **D1** | 真机验收 A，填「二、上次进展」 |
| **D2** | 若通过：Inngest 骨架 + 单事件跑通 1 个 Agent |
| **D3** | 8 Agent 全进队列 + 与 DB 对齐 + 前端轮询策略微调 |
| **D4** | 失败重试、日志、部署环境变量文档 |

（数字仅为建议，按你实际时间推进即可。）

---

## 七、文档索引

| 文档 | 用途 |
|------|------|
| **本文**（[`执行计划.md`](./执行计划.md)） | 总计划、每日习惯、阶段表 |
| [`开发纲要.md`](./开发纲要.md) | **开发目标、阶段约束、v1.2 联调精华、安全前期/中期/后期** |
| [`安全审计与清单.md`](./安全审计与清单.md) | **当前代码安全审计结论、分阶段清单、上线前对表** |
| [`项目功能与架构交接.md`](./项目功能与架构交接.md) | 功能表、架构、路径、库表、环境变量 |
| [`版本与分支.md`](./版本与分支.md) | Git 分支与标签、Inngest 事件名、联调端口备忘 |
| [`多平台App生产工厂路线图.md`](./多平台App生产工厂路线图.md) | **正式路线**：App Spec IR、Flutter/鸿蒙/后端三栈、阶段验收 |
| [`App-Spec-v0.1-草案.md`](./App-Spec-v0.1-草案.md) | Spec 中间表示 v0.1 字段与示例 |
| [`README.md`](../README.md) | 本地运行命令与快速说明 |

---

**记住**：每天开工 = **计划（本文）→ 上次进展（第二节）→ 今天任务 → 收工更新第二节**。
