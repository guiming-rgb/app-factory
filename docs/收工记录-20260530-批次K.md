# 收工记录 — 批次 K（2026-05-30 开工）

## K0 · 门禁

| 命令 | 结果 |
|------|------|
| `verify:i0:batch` | ✅ |
| `verify:i1:flutter` | ✅ |

## K1 · Git

| 项 | 值 |
|----|-----|
| commit | `c8006cb` |
| push | `main` → `origin/main` |

含批次 I/J 探针脚本、Codegen UX、dart-emit、文档与枪战 ID 修正。

## K2 · 鸿蒙 Supabase REST

| 改动 | 说明 |
|------|------|
| `lib/harmony-codegen/emit-entity-list.ts` | `loadItems` + `@ohos.net.http` + PostgREST `/rest/v1/` |
| `templates/harmony-minimal/.../module.json5` | `ohos.permission.INTERNET` |
| `verify:g2:entity-scaffold` | 断言 HTTP / loadItems |

未配置 `SUPABASE_URL` / `ANON_KEY` 时回退静态示例行（与小程序一致）。

## K3 · 生产 HTTP 探针

| 命令 | 结果 |
|------|------|
| `verify:v3:production` | ⚠️ 本环境连 Vercel 超时（exit 2） |
| `verify:s6:local-full`（fallback） | ✅ 3001 + Inngest · 新建项目 8/8 + wechat codegen |

维护者网络正常时可复跑：`npm run verify:v3:production`

## K+部署（续）

| 项 | 值 |
|----|-----|
| 命令 | `npm run deploy:vercel` |
| 部署 | `dpl_JBiWLbLoaofPqx3hGHacude43bii` |
| 别名 | https://app-factory-five.vercel.app |
| 含改动 | 批次 K2 鸿蒙 Supabase REST · I/J 探针与 Codegen UX |

## 一键复验

```bash
npm run verify:i0:batch && npm run verify:g2:entity-scaffold
```
