# 收工记录 — 阶段 D1：后台队列真可用

**日期**：2026-05-28

## 变更

| 项 | 说明 |
|----|------|
| `lib/codegen/inngest-preflight.ts` | 入队前探测 8288 / Cloud 密钥 |
| `lib/codegen/enqueue.ts` | Inngest 未就绪 **fail-fast**（不再留僵尸 queued） |
| `lib/codegen/stale-runs.ts` | queued 15min / running 2h 分项清理 |
| `CodegenPanel` | 90s 排队告警 · Inngest 状态条 · 3min/10min 卡住提示 |
| `/api/deploy/status` | 本地模式实测 8288 是否响应 |
| `npm run dev:codegen:3001` | Next + Inngest 同端口一键启动 |
| `npm run verify:inngest:codegen` | 静态 + 8288 + deploy/status + 可选 async |

## 验收

```bash
npm run build
# 关旧 3001/8288 后：
npm run dev:codegen:3001
npm run verify:inngest:codegen -- --async 802e1144-57ef-4ff4-b33c-325ebad299ce harmony
```

生产：`deploy:vercel` 后 `CODEGEN_E2E_BASE=https://app-factory-five.vercel.app npm run verify:codegen:async -- <id> harmony`

## 运维提示

- **本地**：只能有一个 Inngest Dev（8288）；须与 Next **同端口**
- **Inngest 未开**：点后台生成会立即 **失败** 并提示，不再无限 queued
- **仍 queued**：刷新等 15min 自动清理，或「标记失败」
