# 三阶段执行计划 — 加固 → v5.1 → Codegen 稳定

> **立项**：2026-05-19（维护者拍板）  
> **上位**：[HANDOFF.md](./HANDOFF.md) · [执行计划.md](./执行计划.md) · [v5-记忆与技能-设计草案.md](./v5-记忆与技能-设计草案.md)

---

## 总路线

```text
① 生产 / RLS / 历史数据加固
        ↓
② v5.1 记忆与 Skills 覆盖更多 Agent
        ↓
③ Codegen 从 PoC → 稳定交付
```

---

## 阶段 ① 生产 / RLS / 历史数据加固

| # | 任务 | 状态 | 命令 / 交付 |
|---|------|------|-------------|
| S1 | 文档同步 | ✅ | HANDOFF / ONE_PAGER / 执行计划 / 本文 |
| S2 | 生产 RLS 深测 | ✅ | `verify:v4:production:rls` 双用户通过 |
| S3 | 历史 `owner_id` 策略 | ✅ **策略 B** | 10 项 → `e2e-local@app-factory.test` |
| S4 | Inngest 生产密钥 | ✅ | 生产 `deploy/status` inngest_cloud + anon_key 均 ok |
| S5 | codegen 僵尸清理 | ✅ | `cleanup:codegen:stale` + execute 入口自动清理 |
| S6 | 发版前全链路（可选） | 🔲 | `verify:v3:production` |

**维护者拍板（S3）— 已执行策略 B**：

| 策略 | 行为 |
|------|------|
| **B** ✅ | 10 个 null 项目 → `e2e-local@app-factory.test`（`3697a64b…`） |

若你使用其他登录账号，可 `npm run db:backfill:owners -- --strategy=B --owner=<你的uuid>` 改绑。

---

## 阶段 ② v5.1 记忆与 Skills

| # | 任务 | 状态 | 验收 |
|---|------|------|------|
| V5-6 | 多 Agent 记忆注入 | ✅ | `verify:v5:multi-agent-memories` · CEO/PM/架构/QA |
| V5-7 | 记忆类型 UI（constraint/feedback） | ✅ | 彩色标签 + 重要度文案 · `verify:v5:memories-ui-v7` |
| V5-8 | Skills 管理页 draft→published | ✅ | 管理 UI + `/api/skills/manage` · `verify:v5:skills-admin` |
| V5-9 | 技能注入可观测 | ✅ | usage_logs `skill_injection` + 详情页/API |
| V5-10 | 跨项目用户画像 | 🔲 后置 | v5.1+ |

**V5-6 已注入 Agent**：`ceo` · `product_manager` · `architect` · `qa_lead`

---

## 阶段 ③ Codegen 稳定交付

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| C1 | Report→Spec 收紧 | ✅ | prompt + `normalize-screens/navigation` + `verify:c1:report-to-spec` |
| C2 | Flutter Docker 门禁 + 重试 | ✅ | `CODEGEN_DOCKER_ANALYZE_RETRIES` · 超时退避 |
| C3 | 小程序真编译 | ✅ | wcc/wcsc · `verify:c3:wechat-compile` · 接入 codegen |
| C4 | GitHub push | ✅ | OAuth + 产物 push · `verify:c4:github` |
| C5 | App Spec 阶段 C | 🔲 | 小程序完整 Spec · BackendTarget |

---

## 命令速查

| 意图 | 指令 |
|------|------|
| 批量门禁 | `npm run verify:v4:batch:local` |
| 查 null owner | `npm run db:backfill:owners` |
| 清理僵尸 codegen | `npm run cleanup:codegen:stale` |
| v5-6 检查 | `npm run verify:v5:multi-agent-memories` |
| C1 Spec 收紧 | `npm run verify:c1:report-to-spec` |
| C3 小程序编译 | `npm run verify:c3:wechat-compile` |
| C4 GitHub push | `npm run verify:c4:github` · [C4 指南](./C4-GitHub-OAuth-Push.md) |
| 生产刷新 | `npm run deploy:vercel:env && npm run deploy:vercel` |

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-22 | **C4** GitHub OAuth + 产物 push · **C3** WXML/WXSS 编译 · **C1** Report→Spec ✅ |
| 2026-05-19 | S3 策略 B 回填 · V5-7 UI · C2 Docker 重试 · S2 RLS ✅ · S4 生产 deploy/status ✅ |
