# 收工记录 — 2026-05-19（v2.1 auto-fix + Spec prompt + v3 preview）

> **下次开工**：读 [HANDOFF.md](./HANDOFF.md) · [执行计划.md](./执行计划.md)

---

## 一、今日完成

| 任务 | 结果 | 关键路径 |
|------|------|----------|
| **v2.1 自动修错 Agent** | ✅ | `lib/codegen/auto-fix-flutter.ts` · `execute-flutter.ts` 集成 |
| **收紧 LLM→Spec prompt** | ✅ | `prompts/report-to-spec.ts` · `format-validation-errors.ts` |
| **v3 部署预览 PoC** | ✅ | `/deploy` · `/api/deploy/status` · codegen HTML 预览 API + UI |
| **build** | ✅ | `npm run build` 通过 |

### 环境变量（新增）

见 `.env.local.example`：

- `CODEGEN_AUTOFIX_DISABLED=1` — 关闭自动修错
- `CODEGEN_AUTOFIX_MAX_ROUNDS=3` — 最大修错轮次

### 验收建议

```bash
npm run build
npm run check:deploy
# 双进程后触发 Flutter codegen，完成后详情页应出现「预览」链接
npm run verify:codegen:flutter -- 833ad678-f204-40d7-a47c-5b76e803f64f
```

---

## 二、未做 / 后续

- 小程序 codegen 的 HTML 预览（当前仅 Flutter）
- 小程序 CLI 构建（v2.1 待办剩余项）
- 端到端跑一轮带 analyze 失败的 auto-fix（需 Docker + LLM）

---

## 三、Git

- 勿提交 `* 2.md`、`lib/projects-server 2.ts` 等重复文件
