# 收工记录 — 批次 J（2026-05-30）

## J0 · 门禁

| 命令 | 结果 |
|------|------|
| `npm run verify:i0:batch` | ✅ |
| `npm run verify:i1:flutter` | ✅ |
| `npm run maintainer:pending` | ✅ 0 |

## J1 · 统计

| 命令 | 摘要 |
|------|------|
| `npm run stats:codegen` | flutter 84.6% · wechat 60% · harmony 30.8%（含改同步前历史失败） |
| `npm run stats:codegen:failures` | harmony 多为 Inngest 队列/超时 |

## J2 · 构建

`npm run build` ✅

## J+部署

| 项 | 值 |
|----|-----|
| 命令 | `npm run deploy:vercel` |
| 部署 | `dpl_Hj7S2SBGQ8jP6jR544SCr4XMAa7v` |
| 别名 | https://app-factory-five.vercel.app |

## 并行 · UX

- `CodegenPanel`：默认折叠失败记录；GitHub 仓库 **复制链接**

## 生产枪战 + J4

| 项 | 值 |
|----|-----|
| **正确项目 ID** | `0ea7a53c-a645-4ad9-a43a-02263f9b7b4a`（文档曾误写 `…43c…`） |
| 8/8 | ✅ 本地 Inngest 跑满（原 `running` 无 runs 已 reset） |
| J4 | `verify:v13` 8/8 · `verify:i2:shooter` 三栈 ZIP ✅ |
| Vercel HTTP | Agent 环境连生产 URL 超时；数据与 codegen 经 **同一 Supabase + 本地 3001** 验收 |

## 真源修正

所有文档与 `trigger-project-generate.mjs` 中枪战生产 ID 已统一为 `0ea7a53c-a645-4ad9-a43a-02263f9b7b4a`。

## 未提交

批次 I/J 代码与文档仍在工作区，需维护者 `git commit && git push` 后 Vercel Git 部署与 CLI 部署才长期一致。
