# 收工记录 — 2026-05-31 · 批次 T+（网页 → GHA → Mac/Win 下载）

## 目标

网页点「生成 Flutter」→ 自动跑 GitHub Actions → 回传 Mac/Win 下载链（用户无需本机 Flutter）。

## 已完成

- [x] `lib/github/desktop-gha*.ts` · `desktop-gha-orchestrator` · Inngest `codegen-flutter-desktop-gha`
- [x] `.github/workflows/flutter-desktop-dual-build.yml`（维护者在 GitHub 网页提交，commit `f742759` 等）
- [x] 生产部署多轮，末次约 `e2fd8f7` 起：`sync-desktop-gha` · `Mac .app(GitHub)` · Win 工厂直链
- [x] GHA **#3** 双绿勾：macos 51MB + windows 12.2MB
- [x] **维护者实测**：工厂页 Win 直链可下；Mac 从 Artifacts 安装运行待办 MVP（`app_factory_minimal` / 我的小清单）
- [x] macOS Gatekeeper：未签名 → 右键「打开」可运行

## 生产

- 站：https://app-factory-five.vercel.app
- 枪战/待办项目 Flutter 生成 + GHA 触发已打通

## 已知限制

| 项 | 说明 |
|----|------|
| Mac 工厂直链 | Vercel ~4.5MB 响应上限，Mac ~51MB → 仅 **Mac .app(GitHub)** |
| Inngest 生产 | `.env.local` 无 Cloud 密钥；靠 **刷新记录** `sync-desktop-gha` 回传 Win |
| `git push` workflow | 本地 PAT 缺 `workflow` scope；workflow 靠 GitHub 网页编辑 |
| Mac 签名 | GHA 产物未公证；首次打开需右键打开 |

## 明日可选

1. Vercel 配 `INNGEST_EVENT_KEY` + `INNGEST_SIGNING_KEY`（自动轮询，少依赖刷新）
2. PAT 加 `workflow` scope → 本地可 push workflow
3. 文档：工厂页 Mac/Win 下载说明一页图（给最终用户）

## 验证命令

```bash
npm run verify:t:desktop:build
npm run build
```
