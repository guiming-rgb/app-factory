# 验收 A — 真机联调记录（App 生产工厂）

> **逐步点哪里、开哪个网址**：优先跟 **[验收A-逐步操作指南.md](./验收A-逐步操作指南.md)** 做一遍，再回到本页勾选。  
> **定义**：[执行计划.md](./执行计划.md) 第五节「阶段：验收 A」。  
> **通过标准**：本节「二、数据侧」+「三、产品侧」全部勾选；可选「四、增强」按你产品要求决定。

---

## 一、元信息（收工时填写）

| 字段 | 内容 |
|------|------|
| **验收日期** | **2026-05-19** |
| **执行人** | 维护者真机验收 |
| **分支 / 提交** | `feature/v1.2-inngest`（文档收工时基准 `b98ec3b`，以实际 `git log` 为准） |
| **Supabase** | 项目已配置；执行 `schema.sql` / `seed.sql` |
| **模型** | OpenAI 兼容 API（`.env.local` 中 `OPENAI_BASE_URL` + `OPENAI_MODEL`） |
| **验收用项目 `projects.id`** | 主样本：`58266242-359b-4ae0-a726-fb34929b38a4`（海洋生态）；复测：`c4dc7d7a-7b33-42d8-af8f-09f5350c4de2`（踢足球） |
| **本机端口** | **`http://localhost:3001`**（3000 被其他 App 占用）；`NEXT_PUBLIC_APP_URL=http://localhost:3001` |

---

## 二、环境准备（必做）

- [x] 已在 Supabase 执行 **`sql/schema.sql`**、**`sql/seed.sql`**
- [x] 已配置 **`app-factory/.env.local`**
- [x] **`.env.local` 含 `INNGEST_DEV=1`**
- [x] **`NEXT_PUBLIC_APP_URL`** 与 Next 实际端口一致（本机 **3001**）

---

## 三、双进程与连通性（必做）

**终端 A**

```bash
cd app-factory && npm run build && npm run start -- -p 3001
```

**终端 B**

```bash
cd app-factory && npm run inngest:dev:3001
```

- [x] 终端 A 无报错，浏览器可打开 **`http://localhost:3001`**
- [x] 终端 B 已连接 App；Inngest 本地 UI **`http://localhost:8288`**（以 CLI 打印为准）

---

## 四、产品路径（必做）

1. 首页提交想法（**≥10 字**）→ 进入 **`/projects/[id]`**
2. 点击 **「启动 AI 后台生产」** → 接口 **秒回**（`mode: "async"`），项目进入 **`running`**
3. 等待轮询至 **`completed`**

- [x] 全流程无「长 HTTP 卡死」在 generate 上
- [x] 最终 **`projects.status=completed`**
- [x] 详情页可见完整 **8 Agent** 输出与 **`final_report`**

---

## 五、数据侧检疫（Supabase SQL Editor，必做）

**踢足球样本（2026-05-19 页面确认 8/8）**

```sql
-- 已在联调中确认：status=completed，agent_runs=8，final_report 有实质内容
select id, status, length(coalesce(final_report, '')) as final_report_len
from projects where id = 'c4dc7d7a-7b33-42d8-af8f-09f5350c4de2';

select count(*) as agent_run_rows from agent_runs
where project_id = 'c4dc7d7a-7b33-42d8-af8f-09f5350c4de2';
```

- [x] **A**：`status = 'completed'` 且 `final_report_len` **> 0**
- [x] **B**：`agent_run_rows = 8`
- [x] **C**：成功路径下均为 **`completed`**
- [x] 无异常重复 `agent_runs`（除主动 **`forceRegenerate`**）

**实际查询结果（摘要）**

```
海洋生态 58266242-…：completed，8 runs，报告约 4 万字（API 核对）
踢足球 c4dc7d7a-…：页面 8/8 已完成，历史列表可见
```

---

## 六、v1.1 行为抽测（必做）

- [x] **历史列表**：`http://localhost:3001/projects` 可见样本项目
- [x] **导出 / 复制报告**：详情页按钮可用（已完成项目）
- [x] **409**：`running` 时重复点击 → **「项目正在生成中，请勿重复启动」**（实测）
- [ ] **（可选）错误 Key**：未在本轮执行

---

## 七、v1.2 异步专项（建议做）

- [x] **`POST .../generate`** 秒回，异步模式（页面无长阻塞）
- [ ] **关浏览器后台仍跑完**：未单独记录；以 Inngest **Completed** + 页面 **completed** 为准

---

## 八、工程预检（CI / 收工前可做）

| 命令 | 结果 | 日期 |
|------|------|------|
| `npm run lint` | ✅ 通过 | 2026-05-19 |
| `npm run build` | ✅ 通过 | 2026-05-19 |

---

## 九、验收 B — MVP v1.3（可观测 / usage_logs）

> **详细排查记录**：[验收B-v1.3-问题与结论记录.md](./验收B-v1.3-问题与结论记录.md)  
> **一条命令检验**：`npm run verify:v13`

| 字段 | 内容 |
|------|------|
| **验收日期** | 2026-05-20 |
| **样本 `projects.id`** | `833ad678-f204-40d7-a47c-5b76e803f64f`（少儿踢足球） |
| **Supabase（程序实际连接）** | `dllaezdyxmoebkkwbftd.supabase.co` |
| **通过标准** | `usage_logs` 中该项目 `llm_call` 行数 = **8** |

- [x] 已在**与 `.env.local` 一致**的 Supabase 项目执行 `sql/migrations/20260519_usage_logs.sql`
- [x] `npm run build` 后 `start -p 3001` + `inngest:dev:3001`，重新生成后 `verify:v13` → **✅ 8/8**
- [ ] **验收 B：未通过**

**结论摘要**：此前「看不到 v1.3」主要为 **Supabase 项目混用、未 build、未重新生成**；非核心程序故障。日常以 **`npm run verify:v13`** 为准，不必在详情页找版本号。

---

## 十一、MVP v2（虚拟汇总）— 代码生成双轨

> **定义**：[执行计划.md](./执行计划.md) 里程碑表 **MVP v2**（由 **v2a + v2b** 推导）。  
> **样本 Spec**：`docs/schemas/examples/valid-minimal.json`（`appName`: `kids_soccer`，`displayName`: 少儿足球）。

| 字段 | 内容 |
|------|------|
| **验收日期** | **2026-05-20** |
| **执行方式** | Agent 终端（维护者未手敲） |
| **Git 基准** | `main` @ MVP v2 收工提交（含 v2b 模板 + Generator + 验收记录 §十一） |

### v2a（Flutter）

- [x] `npm run verify:codegen`（= G6）→ Validator ✅ → ZIP → `flutter pub get` → **`dart analyze` No issues**
- [x] 产物示例：`/tmp/kids_soccer-flutter.zip`（约 133 KB）

### v2b（微信原生小程序）

- [x] `npm run verify:wechat-codegen`（= G8）→ Validator ✅ → ZIP → 生成目录 **`verify:wechat` 20/20** ✅
- [x] 产物示例：`/tmp/kids_soccer-wechat.zip`（约 15 KB）
- [ ] **微信开发者工具 GUI**（可选）：导入生成目录，模拟器打开首页 Tab — 未在本轮强制执行

### 汇总

- [x] **MVP v2（虚拟汇总）：通过**（终端门禁口径）
- [ ] **MVP v2：未通过**

**备注**：PoC 口径为 **analyze / 结构门禁**，非 `flutter build apk` 或微信提审；增强见 HANDOFF「v2a/v2b 增强」。

---

## 十、结论（验收 A）

- [x] **验收 A：通过** — 已更新 [HANDOFF.md](./HANDOFF.md)、[执行计划.md](./执行计划.md) 第二节、[CONTINUOUS_DELIVERY_OUTLINE.md](./CONTINUOUS_DELIVERY_OUTLINE.md) §3
- [ ] **验收 A：未通过**

**备注**

```
联调踩坑备忘：
1. 未开 Inngest →「后台任务投递失败：fetch failed」
2. 生产模式详情页曾缓存 pending → 已加 force-dynamic + projects-server 直连库
3. 本机工厂用 3001，须 inngest:dev:3001，勿用默认 3000
历史列表中部分 5/19 失败项为早期未开 B 或投递失败，不影响本次通过结论。
```
