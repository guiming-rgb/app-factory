# 收工记录 — 阶段 B：鸿蒙 C6 产品化 + Codegen 稳定专项

**日期**：2026-05-28

## 鸿蒙 C6 产品化

### 全链路

| 环节 | 变更 |
|------|------|
| DB | `20260528_codegen_harmony_target.sql` — `codegen_runs.target` 含 `harmony` |
| 类型 | `CodegenTarget` = flutter \| wechat \| harmony |
| Inngest | `harmonyCodegen` + `project/codegen.harmony.requested` |
| 执行 | `lib/codegen/execute-harmony.ts`（结构门禁 + Spec 质量 metadata） |
| API | `POST .../codegen/harmony`、`GET .../export-harmony` |
| UI | `CodegenPanel` 鸿蒙按钮；`DownloadHarmonyButton` 同步导出 |
| GitHub | 仓库名后缀 `-harmony` |

### 验收

```bash
npm run db:apply:codegen:harmony   # ✅ 已跑
npm run verify:c6:harmony          # ✅
npm run build                      # ✅
```

### 本地异步 E2E（需双进程 + 重启 Next 含 harmony 路由）

```bash
npm run build && npm run start -- -p 3001
npm run inngest:dev:3001
npm run verify:codegen:async -- 802e1144-57ef-4ff4-b33c-325ebad299ce harmony
```

脚本已支持 **Auth Cookie**（`V4_TEST_EMAIL` / `V4_TEST_PASSWORD`）。

## Codegen 稳定专项

- `assessSpecQuality`：screen/children/tabRoot/entities 映射加强
- `CodegenPanel`：展示 `specQualityScore`、鸿蒙结构门禁标签
- `npm run stats:codegen` — 近 N 天按 target 成功率
- `npm run verify:codegen:stability` — 静态 + 可选 DB 统计

## 生产部署

改 API/Inngest 后需 Redeploy Vercel（无新 env 键）。

**2026-05-28 Redeploy**：`dpl_C5xteiBaWAm7ktXjtn7rSZSqkgGd` → https://app-factory-five.vercel.app

## 异步 E2E 脚本（Auth Cookie）

`scripts/verify-codegen-async.mjs`（已从 `.ts` 改为 `.mjs`，避免 Vercel build 类型检查失败）

```bash
# 本地（需重启 Next 含 harmony 路由 + inngest:dev:3001）
CODEGEN_E2E_BASE=http://localhost:3001 npm run verify:codegen:async -- <projectId> harmony

# 生产
CODEGEN_E2E_BASE=https://app-factory-five.vercel.app npm run verify:codegen:async -- <projectId> harmony
```

依赖 `.env.local`：`V4_TEST_EMAIL` / `V4_TEST_PASSWORD`。
