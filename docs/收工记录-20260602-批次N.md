# 批次 N — 质量冲刺 + P1（2026-06-02）

## N0～N3

| 项 | 结果 |
|----|------|
| `verify:i0:batch` | ✅ |
| `stats:codegen`（30 天） | flutter 85.7% · wechat 71.4% · harmony 47.1% |
| N2 排队 stale | `CODEGEN_QUEUED_STALE_MS` 15min → **90s**（与 UI 一致） |
| N2 鸿蒙路由 | `maxDuration=300`（对齐 flutter/wechat） |
| N3 fallback 文案 | 「待接 Supabase」→「离线示例（拉取失败时显示）」 |
| `verify:g2` | ✅ |

## P1 生产同步验收

```bash
npm run verify:p1:production:sync
```

| 项 | 值 |
|----|-----|
| 项目 | `0ea7a53c-a645-4ad9-a43a-02263f9b7b4a` |
| 栈 | harmony |
| 结果 | `mode=sync` · **completed ~12.7s** |
| runId | `52a0daf7-aba7-41af-81fb-6e2c5183dc5a` |

## 命令

- `npm run verify:p1:production:sync` — 生产 P1 一键复验（需 `V3_HTTP_PROXY` 或 export 7897）
