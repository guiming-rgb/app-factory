# 收工记录 — 阶段 D3 + 三栈 GitHub 自动 push

**日期**：2026-05-28

## D3 固定发版

```bash
npm run release:d3                              # build + 验收 + deploy
npm run release:d3 -- --skip-deploy             # 仅验收
npm run release:d3 -- --push <projectId>        # 发版 + 三栈 push
```

步骤：build → batch:local → inngest:codegen → c6:harmony → github:three-stacks → codegen:stability → deploy:vercel

## 三栈 GitHub 自动 push

| 入口 | 说明 |
|------|------|
| `npm run push:github:three-stacks -- <projectId>` | CLI（PAT 绑定用户） |
| 项目页 **「一键推三栈 GitHub」** | API `POST .../codegen/github-push-all` |
| `ensure: true` | 缺 completed run 时 **同步 codegen** 再 push |

### 样本项目 `802e1144…` 已 push

| 栈 | 仓库 |
|----|------|
| Flutter | https://github.com/guiming-rgb/local_todo-flutter |
| 小程序 | https://github.com/guiming-rgb/simple_todo-wechat |
| 鸿蒙 | https://github.com/guiming-rgb/simple_todo-harmony-harmony（已修 repo 名重复 `-harmony` bug） |

修复后鸿蒙应为 `simple_todo-harmony` — 可再跑一次 push 更新。

## 新增文件

- `lib/github/push-codegen-run.ts`
- `lib/codegen/ensure-completed-runs.ts`
- `app/api/projects/[id]/codegen/github-push-all/route.ts`
- `scripts/push-github-three-stacks.ts`
- `scripts/release-d3.mjs`
