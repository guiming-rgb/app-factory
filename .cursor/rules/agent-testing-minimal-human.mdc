---
description: 测试与验收由 Agent 终端优先执行；事先说明仅维护者能做的项，禁止反复让人手测。
alwaysApply: true
---

# 测试验收 · 少维护者手操（章程）

## 一、原则

- **能自动测的，Agent 必须先测**，再汇报 ✅/❌；不把 Agent 可跑的脚本/命令组甩给维护者「请你试试」。
- **维护者只做 Agent 做不了的事**；事先列清单，不要测到一半才要密码/要人点网页。
- **配套**：[命令组-联调与验收.md](../docs/命令组-联调与验收.md)、[维护者最小参与清单.md](../docs/维护者最小参与清单.md)、[开发纲要.md](../docs/开发纲要.md) §二点五～§二点七。

## 二、开工或宣称「要验收」时 — 先报两句

1. **Agent 将执行**：例如 `G6`/`G8`、`npm run accept`、`build`、已配置的 `verify:*`（写清命令组 ID）。
2. **仅请您配合（若有）**：一次性列完，例如：
   - 首次 `.env.local` 密钥
   - 首次 Supabase SQL 迁移
   - **首次** `git push` / GitHub PAT（Agent 无法代输密码）
   - 可选：微信开发者工具 GUI 导入（终端结构门禁已由 `verify:wechat` 覆盖）

无待办项时写：**「本轮无需您操作，全部由 Agent 终端验收。」**

## 三、Agent 必做（不得省略）

| 场景 | 优先执行 |
|------|----------|
| 阶段收工 | 对应 **G0～G8** 或 `npm run verify:*` / `npm run accept` |
| 改 TS/路由/API | `npm run build`；必要时 `lint` |
| v2a/v2b | `verify:codegen` / `verify:wechat-codegen` |
| **改 codegen / Inngest / Supabase 读写的 API** | **Agent 自启双进程（3001 + `inngest:dev:3001`）→ `POST .../codegen/flutter` → 轮询 `GET .../codegen/runs/[runId]` 至 completed/failed → 断言 `downloadUrl` / `metadata.analyzeStatus`；不把「请你点生成/刷新」甩给维护者** |
| 宣称通过 | 贴终端 **最后 5～15 行** + API 断言摘要；更新 `验收记录.md` / `HANDOFF.md` |

**禁止**：未跑门禁就写「请您本地验证」；把 5～6 条命令拆成多轮让维护者逐条敲；改完 codegen 只叫维护者「硬刷新看看」而不自测 API。

## 四、仅维护者 / 须事先说明的项

| 项 | 为何 Agent 不能代做 | Agent 应做 |
|----|-------------------|------------|
| GitHub PAT / `git push` 密码 | 无交互凭据、非交互 Shell | 配好 `http.version`/代理端口后，**一次性**说明「请您终端执行 `git push` 并输入 PAT」 |
| `.env` 密钥 | 安全 | 只检查 `test -f .env.local`，不索要密钥内容 |
| Supabase 首次 SQL | 控制台权限 | 给出迁移文件路径，不让人反复对表 |
| 微信开发者工具 GUI | 无 GUI | `verify:wechat` 通过后，标注 GUI 为**可选一次** |

## 五、失败时

- Agent **先**换端口/代理/重跑命令组/读日志，再请示维护者。
- 向维护者索取时，必须写：**缺什么、为何 Agent 不能代做、您只需做哪一条命令**。
